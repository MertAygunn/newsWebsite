import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['i.ytimg.com', 'img.youtube.com'], // Her iki YouTube resim alan adını buraya ekleyin
  },
};

export default nextConfig;